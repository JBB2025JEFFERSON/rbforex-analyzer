{
  "name": "rbforex-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor local que serve o RBFOREX Analyzer e faz proxy das chamadas para football-data.org (evita CORS).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
